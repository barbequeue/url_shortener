<div class="w-100 new-link">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-8">
                <h1 class="font-weight-normal mt-4 text-center">URL-shortener</h1>
                <%= render 'form' %>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <%= render 'table' unless @links.empty? %>
            </div>
        </div>
    </div>
</div>
<p class="position-absolute bg-secondary text-white px-3 py-2" style="top: 0; left: 0;">
<b>current user</b>: <%= @user.session_id %><br>
<b>cookies</b>: <%= @session %>
</p>
